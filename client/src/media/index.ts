export type { MediaDifference, MediaIdentity } from './types';
export { compareMediaIdentity, createMediaIdentity, formatBytes } from './fingerprint';
export { readVideoDuration } from './videoMetadata';
